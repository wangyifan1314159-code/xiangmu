# 项目迭代日志

> 记录人机协作迭代过程：安全审查与修复、部署加固、监控大屏重构与真实数据接入
> 仓库：wangyifan1314159-code/xiangmu（main 分支）

---

## 2026-08-28 · 迭代五：全面接口与功能检查 + 假数据可见性修复

**背景**：四专项检查（接口审计/大屏假数据根因/无 TCP 数据更新/TCP 状态虚假显示）后执行 P0 修复。

**检查报告结论**（详见会话记录）：
- iot-data-service 模块（8082）前端零调用，处于休眠状态
- 双模网关 mock 降级静默吞错，页面无法分辨真假数据
- TCP 假连接两个根因：Clash TUN 代理代答 TCP 握手使 Netty connect 误报成功；后端不可达时 mockApi 伪造 192.168.1.x 连接列表
- 无 TCP 数据仍更新：前端 3s mock 定时器为主因（与后端无关）

**修复内容**：
1. **后端 TCP 活性判定**（`TunnelingMachineTcpClientConnectionManager.java`）：快照状态由「通道活跃即 CONNECTED」改为三态——通道活跃且 30s 内有有效帧 → `CONNECTED`；通道活跃但无帧（代理代答握手的假连接）→ `NO_DATA`；无通道 → `RECONNECTING`。新增 `STALE_FRAME_MILLIS=30s` 常量与 `Session.resolveStatus()`
2. **mockApi TCP 假数据清空**：`getTcpStatus` 返回 `{enabled:false, onlineDevices:0}`，`getTcpConnections` 返回空列表——后端不可达时显示空而非伪造连接
3. **TcpDeviceConnectView 状态三态显示**：新增「已连接·无数据」黄色标签（NO_DATA），「已连接」绿色、「重连中」灰色
4. **TcpConnectionsView 演示模式横幅**：`isInFallback()` 为真时顶部显示「后端不可达，当前为演示模式」警告条
5. **大屏数据源徽标**：顶栏右侧新增实时状态徽标（3s 刷新）——WS 已连且未降级 → 绿色「实时数据」；否则橙色「离线·演示数据」，杜绝「数字在动=系统正常」误判

**验证**：iot-backend `mvn test` 通过（exit 0）；前端 `npm run build`（vue-tsc + vite）通过。

**未处理**（后续迭代）：iot-data-service 去留决策、DeviceListView 演示横幅、connect() 异步化、SENSOR_TYPE_MAP 扩展 methane/wind_speed。

---

## 2026-08-28 · 迭代四：大屏接入真实后端数据（commit `74a9aec`）

**目标**：监控大屏由纯 mock 数据切换为真实数据驱动。

**改动**（`vite-project/src/views/dashboard/DashboardView.vue`，+155/-41）：
- 新增 `SENSOR_TYPE_MAP` 传感器类型映射表：temperature/humidity 直连；pm25/co2 以 scale 系数代理映射至粉尘/CO 显示（代码注释标明为量级代理）
- WebSocket 推送按 sensorId 类型分发至「巷道环境监测」与「巷道态势图点位」
- 新增 `realUntil` 15 秒豁免机制：真实值到达即覆盖显示并暂停 mock 游走，断流后自动恢复 mock 兜底，保证大屏不空
- 设备类型分布改为 `deviceStore.devices` 真实聚合（按 type 计数取前 6，空列表回落 mock）
- 底部趋势温度序列用 `getDeviceData` 拉取 60 条真实历史预热，X 轴时间戳重建
- 首屏 `seedRealInitialValues()`：命中映射的传感器当前值直接呈现

**仍为 mock**（后端无对应接口）：安全运行天数、系统节点状态、PHM 健康指数、智能联动事件、CH4/风速/掘进机指标。

**验证**：`npm run build`（vue-tsc + vite）通过。

---

## 2026-08-28 · 迭代三：大屏 DataV 视觉升级 + 全屏修复（commit `2b5b430`）

**目标**：按主流大数据平台风格（阿里 DataV 政企指挥风）重设计视觉，修复全屏 bug。

**视觉**（单文件 +187/-88，未动全局 tokens.css）：
- 色板切换：深海军蓝 `#0a1e3c` 底 + 径向渐变 + 微网格底纹，荧光青 `#00E5FF` 主色
- 面板标题栏斜切平行四边形装饰 + 渐变底线 + 文字发光；四角 sci-fi 角标加光晕
- 顶部主标题左右对称科技装饰线 + 白→浅青渐变文字
- 扫描线动效（巷道面板 8s / 报警卡 6s 红色），报警卡光晕增强
- echarts 全部图表与巷道 SVG 换至新青蓝色系，语义色（绿/橙/红）保持

**全屏修复**（根因修复）：
- 原 bug：`document.documentElement.requestFullscreen()` 全屏整个文档，侧边栏/顶栏残留
- 修复：改为大屏根元素 `requestFullscreen()` + `:fullscreen` CSS 铺满 100vw/100vh + `::backdrop` 防白边，进出全屏后 echarts 自动 resize

**验证**：`npm run build` 通过。

---

## 2026-08-28 · 迭代二：监控大屏方案 B 重构（commit `00e8614`）

**目标**：原大屏不符合大数据平台展示规范（彩虹配色、装饰性雷达图、静态图片低密度、X 轴时间标签重复堆积），按经典指挥大屏重构。

**改动**（`DashboardView.vue` 1833 行 → 1425 行重写）：
- 布局：纵向堆叠 → 左中右三栏（22/52/26，1920×1080 基准 grid 自适应）
- 中央主视觉：纯 SVG 巷道态势图——巷道剖面、EBZ-260 掘进机剪影+进尺标注、8 个脉冲传感器点位（随机游走+间歇预警变色）、风筒风流动画、悬停 tooltip
- 顶部 8 卡 KPI 带（待响应报警红色呼吸强调）+ 告警无缝滚动条
- 左栏：巷道环境 6 项分段进度条 / 掘进进尺 gauge / 设备类型分布条形图（替代彩虹饼图）
- 右栏：PHM 健康指数 / 十米级遥测数据流 / 智能联动中心
- 底部三序列双轴面积趋势，修复 X 轴时间标签重复堆积硬伤（类目轴 + 整分钟标记过滤）
- 移除装饰性雷达图，色板收敛为青蓝单色系

**数据策略**：先视觉后接数——mock 随机游走为主，标注 `[接入点]` 注释；设备数/告警滚动/WS 遥测 3 处保持真实接入。

**备份**：原页面存至 `原UI/backup-dashboard-20260827/DashboardView.vue`。

**验证**：`npm run build` 通过。

---

## 2026-08-28 · 迭代一b：修复部署脚本编码问题（commit `7b3ef85`）

**问题**：`deploy.ps1` 运行报十余个 PowerShell 解析错误。

**根因**：安全修复时脚本被保存为 UTF-8 无 BOM，Windows PowerShell 5.1 按 ANSI/GBK 读取，中文乱码并"吞掉"后续 ASCII 引号，导致字符串失去终止符引发连锁解析错误。

**修复**：补加 UTF-8 BOM（文件头 `EF BB BF`）。验证：解析器 0 错误，`.\deploy.ps1 status` 正常执行。

---

## 2026-08-28 · 迭代一：安全审查与漏洞修复（commit `83b231e`）

**背景**：MetaGPT 多角色（架构师/工程师/QA）并行安全审查，发现 5 高危 + 8 中危 + 多项低危。

### 高危修复
| 漏洞 | 修复 |
|---|---|
| 一键部署默认种入弱口令 admin/admin123 | compose 种子开关默认 false，deploy 脚本删除明文口令横幅 |
| MQTT 命令信道无设备级认证，可冒充平台控制执行器 | 新增 `APP_MQTT_COMMAND_TOKEN` 校验，fail-closed（未配置即拒绝命令） |
| EMQX 凭证硬编码公开值 iotplatform2024 / Dashboard admin/public | 改为 `:?` 强制注入，deploy 脚本随机生成（openssl rand / GUID） |
| 大数据栈裸奔（Redis 无密码、Kafka PLAINTEXT、Flink UI 无鉴权、TDengine 默认口令，全部 0.0.0.0 暴露） | 9 处端口绑定 127.0.0.1、Redis requirepass、Kafka 关自动建 topic、MinIO 锁定版本 |
| iot-data-service 全端点匿名可访问 | 引入 Spring Security + JWT（复用 `APP_JWT_SECRET`，密钥 <32 字节 fail-fast） |

### 中危修复
- sensorId 归属校验（防跨设备篡改传感器数据 IDOR，新增 `SensorRepository.existsByIdAndDevice_DeviceId`）
- Redis 反序列化去除 Jackson default typing（`GenericJackson2JsonRedisSerializer` → 无类型 `Jackson2JsonRedisSerializer`）
- WebSocket 订阅默认拒绝（白名单未命中即拒）
- 手机验证码自动注册派生标识符唯一化（SecureRandom 后缀，防预占拒绝登录）
- deploy.ps1 JWT secret 改用 CSPRNG（`RandomNumberGenerator`）替代 `Get-Random`

### 核查无问题项
SQL 注入（TDengine 白名单齐全）、告警/设备 IDOR、密码存储（BCrypt）、JWT 密钥管理、Actuator 暴露面、CORS 默认值、Flink 反序列化。

### 验证
`mvn test` 全模块通过（含 Flink 基准 117,998 EPS）；前端 `npm run build` 通过。共 17 文件 +323/-45。

**部署注意**：重新部署前需在 `.env` 设置 `EMQX_MQTT_PASSWORD`、`EMQX_DASHBOARD_PASSWORD`、`REDIS_PASSWORD`（deploy 脚本自动生成）；iot-data-service 启动需注入与 iot-backend 相同的 `APP_JWT_SECRET`。

---

## 遗留事项（未完成）

- JWT 撤销/黑名单机制（登出后 token 仍有效至过期）
- 登录防爆破分布式限流（当前为单机内存实现）
- 设备 API Key 加密存储（当前明文落库）
- 全链路 TLS（MQTT/Kafka/JDBC/TCP 均明文）
- 前端 token 迁移 HttpOnly Cookie、mockApi 生产构建隔离
- 大屏剩余 mock 模块的后端接口（PHM/联动/系统节点/QPS 统计）

## 工作区未入库内容

`iot-backend/src/main/resources/static/` 下存在前端构建产物（部署流程拷贝生成），是否入库待定。
