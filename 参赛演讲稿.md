# 《让每一份数据真实地说话》
## ——工业物联网大数据与实时湖仓平台参赛演讲稿

> 时长：约 20 分钟（全文约 4700 字，按每分钟 230 字设计）
> 说明：所有技术细节（端口、阈值、参数）均来自项目真实代码，可随时以代码佐证。

---

## 【开场 0:00–1:30】

尊敬的各位评委、各位老师，大家好！

在开始之前，我想请大家想象一个场景：深夜的矿井巷道里，一台掘进机正在作业，甲烷传感器的读数突然"卡住"了——不是危险解除了，而是传感器坏了。如果监控大屏上显示的正是这个假读数，后果会怎样？

我们团队要解决的，正是这个问题：**如何让工业设备的每一份数据，都真实、及时、可信地"开口说话"。**

我们带来的作品，是一套"物联网大数据与实时湖仓一体化平台"。接下来的二十分钟，我会从行业痛点讲起，带大家完整走一遍数据从设备诞生、到流入平台、再到呈现到大屏的全过程。所有的技术细节，都来自我们真实运行的代码。

---

## 【行业背景与痛点 1:30–4:00】

工业物联网领域，长期存在四个"老大难"问题。

**第一，接入难。** 现场设备五花八门：新设备支持 MQTT，老设备只有 TCP 裸口。很多平台只支持一种协议，剩下的设备就成了"数据孤岛"。

**第二，数据脏。** 传感器会老化、会卡死。业内有个术语叫 Flatline——平线故障，传感器连续输出一模一样的读数。表面上"一切正常"，实际上数据早已失真。用脏数据做决策，比没有数据更危险。

**第三，存储贵。** 遥测数据是典型的时序数据，量大、写入频繁。全部放在高价的高速存储里，成本扛不住；全部放冷存储，查询又慢。数据天生需要"分屋居住"。

**第四，链路假。** 很多演示系统的大屏很漂亮，但数据是定时器模拟出来的。评委看到的是动画，不是真实系统。我们给自己立了一条铁律：**全链路真实数据，零模拟。**

围绕这四个痛点，我们设计了这个平台。下面进入正题。

---

## 【总体架构总览 4:00–7:00】

我们采用 Maven 多模块架构，四个后端模块加一个前端，基于 Java 17 构建。

- **iot-backend**，业务主服务，承担设备管理、用户认证、告警规则，同时内置 TCP 设备接入通道；
- **iot-flink-jobs**，实时流计算引擎，是整个平台的数据"质检员"和"计算器"；
- **iot-data-service**，OneData 统一查询中台，对外提供 OLAP 分析接口；
- **iot-common**，公共模型层，保证各模块间消息结构一致；
- **vite-project**，Vue 3 前端，包含监控大屏与管理界面。

底座组件通过 Docker Compose 一键编排：EMQX 5.8 做 MQTT 接入、Kafka 3.9 做消息中枢、Flink 1.18 做流计算、PostgreSQL 存业务元数据、Redis 做实时缓存、TDengine 与 MinIO 加 Iceberg 承担时序与湖仓存储。

整条数据链路一句话概括：**设备数据从 TCP 或 MQTT 双通道进来，先进 Kafka 缓冲解耦，再由 Flink 清洗、聚合，按温度分层落入存储，最后通过 WebSocket 实时推上大屏。**

这个架构的精髓在于"解耦"二字。设备接入、数据缓冲、流计算、存储查询、前端展示，每一层都可以独立扩容、独立故障隔离。设备端再大的并发，也不会直接压垮业务后端。

---

## 【亮点一：双通道接入与安全认证 7:00–9:30】

先看数据是怎么进来的。

**第一条通道是 TCP 长连接。** 我们基于 Netty 实现了 JSON 行协议，默认端口 1884。这里有个设计细节值得一提：我们支持两种鉴权模式。单台设备逐台认证是基础模式；但我们更推荐**网关模式**——一个网关用访问令牌只认证一次，就可以携带多台设备的遥测数据，而且平台只接受认证清单内的设备。这样既减少了握手开销，又杜绝了设备身份伪造。

**第二条通道是 MQTT。** 我们部署 EMQX 5.8 作为 Broker，启用了内置数据库认证，SHA256 加密存储口令，**彻底关闭匿名连接**。平台侧和设备侧各自持有独立凭据。

两条通道最终汇入 Kafka 的遥测主题 device.telemetry.v1。Kafka 在这里扮演"蓄水池"的角色：前端洪峰来了，它先兜住；下游消费速度波动，它来缓冲。我们关闭了自动创建 Topic，任何生产者想写入，必须先走规划审批，避免消息总线失控。

还有一条反向链路——**指令下发**。平台向设备下发控制命令时，系统自动选择通道：设备 TCP 在线，就走 TCP 直连，延迟最低；不在线，自动降级走 MQTT 兜底。前端只需调用一个接口，通道选择对用户完全透明。

---

## 【亮点二：Flink 实时流计算与数据质量清洗 9:30–13:00】

数据进了 Kafka，就轮到我们平台的"心脏"——Flink 流计算作业登场了。这是我今天最想展开讲的部分。

**第一步，治理乱序。** 网络传输中，后产生的数据可能先到。我们用事件时间语义配合水位线，允许 5 秒的乱序容忍度，并为空闲分区设置了 60 秒的空闲检测，避免个别静默设备拖住整个窗口的推进。

**第二步，质量清洗。** 这是 TelemetryQualityProcessFunction 的核心工作，它做了三道检查：

第一道，**无效值过滤**——空值、NaN、无穷大，直接打入脏数据旁路流；

第二道，**物理极值过滤**——比如温度传感器，读数低于零下 100 度或超过 1500 度，物理上不成立，判定为野值拦截；

第三道，也是最精彩的，**死值检测**。我们对每个"设备加传感器"的组合维护键控状态：上一次读数、相同读数的连续次数。一旦连续 20 个采样点读数完全一致，就判定传感器疑似卡死。这时数据**不再进入下游聚合**，避免污染统计结果，同时发出死值告警。

这里有两个工程细节，是我们反复打磨出来的：其一，**告警防风暴**——每个平线周期只告警一次，后续相同读数只拦截不重复告警，直到读数真正变化才复位；其二，**状态生命周期管理**——我们给键控状态设置了 30 分钟的 TTL 和增量清理策略，设备停更后状态自动回收，不会随着接入设备增多而内存膨胀。计数器还做了封顶处理，防止长期平线导致计数溢出。**一个不到百行的函数，体现了我们对流式状态管理的完整理解。**

**第三步，迟到数据处理。** 超过水位线容忍度才到的数据，我们不做简单丢弃，而是通过侧输出流分流到独立的迟到数据主题，保留补救和分析的余地。

**第四步，窗口聚合。** 清洗后的干净数据进入 1 分钟滚动窗口，按设备、传感器聚合出均值、最大、最小值和样本数，写入聚合主题，供下游中台消费。脏数据则单独流向告警主题。作业开启了 30 秒一次的检查点，故障重启后状态不丢、数据不重。

---

## 【亮点三：湖仓分层存储与 OneData 中台 13:00–15:30】

数据洗干净了，存到哪里？我们的答案是：**按温度分层居住。**

**热数据**——最新状态和实时查询，放 Redis 设备影子和 TDengine，支撑毫秒级响应；
**温数据**——近中期聚合分析，放 Doris 的 ADS 聚合层，支撑多维即席查询；
**冷数据**——历史归档，放 MinIO 对象存储加 Iceberg 表格式，Parquet 列存，成本大幅下降。

分层之后，谁来统一供数？这就是我们的 **iot-data-service 中台**。它监听 Flink 输出的聚合主题，在内存中维护每个传感器最新的聚合指标和滑动历史，对外提供三类能力：

一是**多维指标即席查询**，并附带真实测量的端到端延迟——这个延迟是怎么算的？当前系统时间减去聚合窗口的结束时间，是真实测量值，不是拍脑袋写的常数；

二是**设备健康度评分**，基于最新聚合指标对设备做 PHM 视角的健康评估；

三是为生产大屏提供统一的实时数据 API。

中台的意义在于：**下游要什么数据，只找 OneData；底层数据源怎么换、怎么扩容，下游无感知。**

---

## 【亮点四：真实数据驱动的大屏 15:30–17:00】

最后数据来到了大屏。这里我想讲一个我们的"自我革命"。

项目早期，大屏里有不少模拟定时器在"制造"漂亮数据。在功能检查中，我们毅然删掉了全部模拟数据源，确立了 **WebSocket 作为大屏唯一数据源**的原则：后端真实收到 TCP 数据并推送，大屏显示绿色"实时数据"标识；连接断开或无真实数据，就诚实显示橙色"离线"和空态提示，绝不造假。

我们还做了"实时数据管道"面板：按传感器类型展示消息分布环形图、累计消息数、基于 60 秒滑动窗口的消息速率，管道状态一目了然。评委在答辩现场看到的每一次数字跳动，都是设备真实发上来的数据。

---

## 【工程实践与安全加固 17:00–18:30】

一个优秀的作品，不仅要功能强大，更要工程规范。

**安全方面**，我们做了系统性加固：所有数据库密码、JWT 密钥、EMQX 和 Redis 口令一律从环境变量注入，缺失即拒绝启动；数据库和管理面端口只绑定本机回环地址；默认种子账号关闭；口令使用 BCrypt 哈希存储；设备 API Key 配合过滤器做设备级鉴权。我们做过一轮完整的安全评审，对发现的弱口令、未认证端点等问题逐项整改闭环。

**质量方面**，核心逻辑都有单元测试覆盖：帧解码器、TCP 消息处理、告警服务、设备服务，均有对应测试类。

**部署方面**，一键部署脚本整合了前端构建、后端打包、服务启动与健康检查。我们甚至踩过并修复了 PowerShell 脚本编码导致的部署故障——这些真实踩坑经历，都是团队最宝贵的财富。

---

## 【攻关故事与结尾升华 18:30–20:00】

最后，分享一个让我们印象最深的攻关故事。联调阶段，平台总误报设备"在线"，排查后发现是本机代理软件的 TUN 模式伪造了 TCP 连接成功。我们没有回避，而是给连接状态增加了"最后收帧时间"的活动性校验——**只有真正收到数据帧，才算真在线。** 这个案例让我们深刻体会到：工业系统的可靠性，藏在每一个"眼见为实"的细节里。

各位评委，从设备接入到流式清洗，从湖仓分层到真实大屏，我们构建的不只是一套系统，更是一种信念：**数据可以迟到，但不能说谎。**

以真实数据驱动真实决策——这就是我们献给工业智能化的一份答卷。

我的演讲到此结束，谢谢大家！

---

## 附：演讲技巧备忘

| 环节 | 要点 |
|------|------|
| 开场场景 | 放慢语速，营造画面感，这是抓住评委的关键 30 秒 |
| 亮点二 | 全场技术核心，讲到"三道检查"时可伸出手指计数 |
| 真实数据承诺 | "零模拟""数据不说谎"是全篇记忆点，务必加重语气 |
| 攻关故事 | 用讲故事的口吻，展现真实工程能力，评委最买账 |
| 控场 | 每段末尾的时间点是硬边界，练习时用秒表校准 |

## 附：答辩防追问速查（代码出处）

| 演讲中的说法 | 代码出处 |
|--------------|----------|
| TCP 端口 1884、JSON 行协议 | README.md / application.yml（app.tcp.port） |
| 网关一次认证、白名单设备 | TcpMessageHandler / TunnelingMachineTcpClientConnectionManager |
| 水位线 5 秒乱序、60 秒空闲检测 | TelemetryStreamApp.java |
| 三道质量检查（无效值/极值/死值 20 点） | TelemetryQualityProcessFunction.java |
| 状态 TTL 30 分钟、计数器封顶 20 | TelemetryQualityProcessFunction.java |
| 1 分钟滚动窗口聚合、30 秒检查点 | TelemetryStreamApp.java |
| 端到端延迟 = 当前时间 − 窗口结束时间 | OlapAnalyticsService.java |
| 热温冷三层存储 | README.md 核心大数据特性 |
| EMQX 禁匿名、SHA256、口令环境变量注入 | docker-compose.yml / docker-compose-bigdata.yml |
| WebSocket 唯一数据源、绿/橙状态标识 | vite-project/src（websocket store、大屏视图） |
